'use strict';

const { extractCommandFromArguments } = require('./extractCommandFromArguments');
const { extractEnvironmentFromArguments } = require('./extractEnvironmentFromArguments');

module.exports = {
  extractCommandFromArguments,
  extractEnvironmentFromArguments,
};
